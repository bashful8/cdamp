// PLACEHOLDER — not a real htmx build.
//
// STATUS.md's Phase 6 task 5 spec calls for vendoring a real htmx release
// under this path (go:embed'd, served at /dashboard/static/htmx.min.js) so
// the dashboard has no runtime dependency on an external CDN. This builder
// pass has no internet access and cannot fetch an actual htmx build, so
// this file is a placeholder, not a functioning htmx implementation.
//
// It is safe to ship as-is for this task specifically because the only
// view built here (GET /dashboard, agents.html) is plain server-rendered
// HTML with no hx-* attributes and no htmx-driven interactivity yet — the
// <script> tag loading this file is present for future views (per-agent
// inbox, thread view, search) that will need it, per this task's spec.
//
// Flagged as a non-blocking deviation in this task's builder report.
// Proposed fix: swap this file for a real vendored htmx release (e.g.
// htmx.org's dist/htmx.min.js, any recent stable version) in a follow-on
// task, once network access to fetch it is available.
